<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@drawable/dialog_bg"
    android:orientation="vertical"
    android:padding="24dp">

    <TextView
        android:id="@+id/tvPaidServerTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/vsm_server_dialog_title"
        android:textColor="@color/vsm_restore_card_title"
        android:textSize="16sp"
        android:textStyle="bold" />

    <TextView
        android:id="@+id/tvPaidServerMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/vsm_server_dialog_body"
        android:textColor="@color/vsm_restore_card_body"
        android:textSize="13sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:gravity="end"
        android:orientation="horizontal">

        <Button
            android:id="@+id/btnPaidServerCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/cabinet_outline_button_bg"
            android:minHeight="38dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:text="@string/vsm_server_dialog_cancel"
            android:textAllCaps="false"
            android:textColor="@color/vsm_link"
            android:textSize="14sp"
            android:textStyle="bold" />

        <Button
            android:id="@+id/btnPaidServerPay"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="9dp"
            android:backgroundTint="@color/vsm_pay_btn"
            android:minHeight="38dp"
            android:paddingStart="14dp"
            android:paddingEnd="14dp"
            android:text="@string/vsm_server_dialog_cta"
            android:textAllCaps="false"
            android:textColor="@android:color/white"
            android:textSize="14sp"
            android:textStyle="bold" />
    </LinearLayout>

</LinearLayout>
